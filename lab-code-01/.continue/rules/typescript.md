---
name: typescript
description: TypeScript Standards
globs: ["**/*.ts", "**/*.tsx"]
version: 1.0.0
invokable: true
---
Whenever I am writing TypeScript:
- Prefer interface over type where possible.
- Use strict null checks.
- Avoid the `any` type; use `unknown` if necessary.
